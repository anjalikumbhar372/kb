apiVersion: v1
kind: service
metadata:
  name: apache-svc
  labels:
    app: apache
    namespace: apache
spec:
  type: ClusterIP
  selector:
    app: apache
    ports:
      - port: 80
        targetPort: 80