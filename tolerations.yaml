apiVersion: v1
kind: pod
metadata:
  name: mypod
  labels:
    app: myapp
spec:
  tolerations:
    - key: "prod"
      operator: "Equal"
      value: "true"
      effect: "NoSchedule"
  containers:
    - name: nginx-cont
      image: nginx:latest
      ports:
        - containerPort: 80
  